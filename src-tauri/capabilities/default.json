{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core default permissions. HID and file read/write are app-specific commands so no extra permissions are needed; only the save/open dialogs require the plugin-dialog permission",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
